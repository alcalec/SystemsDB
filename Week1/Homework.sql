select current_date, current_database(), version(), 'Hello, SQL!';
/*
- Use Create Database (name); command to create a new database
- Use Create Table (name); command
  * bigserial is an integer that auto-increments- Postgre specific
  * varchar is a text column, includes parentheses for amount of characters
- Semicolons signal the end of command, part of the ANSI SQL standard, good practice as well
- date data type allows a date to be typed and shown
- salary date type is numeric type to be inserted and shown
*/
